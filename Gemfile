# Vesper Atelier — Gemfile (développement local uniquement)
# Le build de GitHub Pages utilise sa propre chaîne (github-pages) ;
# ce fichier sert au lancement local : `bundle install` puis `bundle exec jekyll serve`.

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
